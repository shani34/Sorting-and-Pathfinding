const compareColor = "#FFFF00" // intial compare colour yellow
const swapColor = "#FF0000";  // swaping color is red
const defaultColor = "#FFFFFF"; // default color is white
const sortedColor = "#228B22";
const overwriteFromMemoryColor = "#ffadad";
const lessThanPivotColor = "#9bf6ff";

export default class SortingColors {
    static get compareColor() {
        return compareColor;
    }

    static get swapColor() {
        return swapColor;
    }

    static get defaultColor() {
        return defaultColor;
    }

    static get sortedColor() {
        return sortedColor;
    }

    static get overwriteFromMemoryColor() {
        return overwriteFromMemoryColor;
    }

    static get lessThanPivotColor() {
        return lessThanPivotColor;
    }
}